{
  "name": "aether-forge",
  "version": "0.1.0",
  "private": true,
  "description": "Private multi-agent OS for enterprise knowledge work",
  "scripts": {
    "dev": "npm run dev:web",
    "dev:web": "npm run dev --workspace=@aether-forge/web",
    "build": "npm run build --workspace=@aether-forge/web",
    "test": "echo \"TODO\"",
    "demo": "bash demo.sh",
    "start:orchestrator": "tsx src/orchestrator.ts",
    "start:api": "tsx src/api.ts"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "engines": {
    "node": ">=20"
  }
}
