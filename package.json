{
  "name": "aether-forge",
  "version": "0.1.0",
  "private": true,
  "description": "Private multi-agent OS for enterprise knowledge work",
  "scripts": {
    "dev": "echo \"TODO: wire monorepo scripts\"",
    "build": "echo \"TODO\"",
    "test": "echo \"TODO\"",
    "start:orchestrator": "tsx src/orchestrator.ts"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "engines": {
    "node": ">=20"
  }
}
